services:
  - type: web
    name: flask-app
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libreoffice  # Install LibreOffice for Linux PDF conversion
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: "gunicorn app:app"
    plan: free
    autoDeploy: true
